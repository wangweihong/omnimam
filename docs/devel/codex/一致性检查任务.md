请检查当前代码和 docs 设计文档是否一致。

要求：

1. 扫描最近的 git diff。
2. 检查新增、修改、删除的前端页面、后端接口、数据库结构。
3. 对比 docs/module-manifest.yaml、docs/architecture/module-index.md、docs/modules/*.md。
4. 找出以下问题：
   - 代码已实现但文档未记录
   - 文档记录为可用但代码缺失
   - 接口路径不一致
   - 页面路由不一致
   - 数据库字段不一致
   - 功能状态不准确
5. 输出修复建议。
6. 如果我确认，请再同步修改文档。