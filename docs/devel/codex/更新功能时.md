请基于当前 git diff，同步更新系统设计文档，不要修改业务代码。

要求：
1. 检查本次变更涉及哪些前端页面、后端接口、数据库结构、配置或权限。
2. 更新 docs/generated 下的事实清单。
3. 更新 docs/architecture 下的总设计文档。
4. 更新对应 docs/modules/{module-id}.md。
5. 更新 docs/module-manifest.yaml。
6. 在 docs/architecture/change-log.md 追加记录。
7. 所有状态判断必须包含代码证据。