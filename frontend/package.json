{
  "name": "omnimam-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run build --workspace @omnimam/web",
    "dev": "npm run dev --workspace @omnimam/web",
    "typecheck": "npm run typecheck --workspace @omnimam/web"
  }
}
